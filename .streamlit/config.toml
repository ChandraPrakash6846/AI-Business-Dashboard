[theme]
primaryColor = "#667eea"
backgroundColor = "#0e1117"
secondaryBackgroundColor = "#1a1c24"
textColor = "#e0e0ff"
font = "sans serif"

[layout]
# Use wide mode by default, but we'll control it in app.py too
